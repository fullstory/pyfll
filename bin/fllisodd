#!/usr/bin/python3 -tt
# -*- coding: utf-8 -*-

import os
import sys

try:
    from pyfll.isodd import main
except ModuleNotFoundError:
    sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), ".."))
    from pyfll.isodd import main

main()
