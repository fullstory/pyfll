set menu_color_normal=cyan/blue
set menu_color_highlight=white/blue

menuentry "Back to Localisation Menu" {
    configfile $prefix/grub.locale.cfg
}

menuentry "Default By Language" {
    unset kbd
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Belgian" {
    set kbd="keytable=be"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Canadian (Multilingual)" {
    set kbd="keytable=ca"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Croatian" {
    set kbd="keytable=hr"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Czech" {
    set kbd="keytable=cz"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Danish" {
    set kbd="keytable=de"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Dutch" {
    set kbd="keytable=nl"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Dvorak" {
    set kbd="keytable=dvorak"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "English (IE)" {
    set kbd="keytable=ie,gb"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "English (UK)" {
    set kbd="keytable=gb"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "English (US)" {
    set kbd="keytable=us"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Estonian" {
    set kbd="keytable=et"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Finnish" {
    set kbd="keytable=fi"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "French" {
    set kbd="keytable=fr"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "French (Canada)" {
    set kbd="keytable=cf"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "German" {
    set kbd="keytable=de"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Greek" {
    set kbd="keytable=gr"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Hungarian" {
    set kbd="keytable=hu"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Icelandic" {
    set kbd="keytable=is"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Italian" {
    set kbd="keytable=it"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Japanese" {
    set kbd="keytable=jp"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Korean" {
    set kbd="keytable=kr"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Lithuanian" {
    set kbd="keytable=lt"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Norweigan" {
    set kbd="keytable=no"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Polish" {
    set kbd="keytable=pl"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Portuguese (PT)" {
    set kbd="keytable=pt"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Portuguese (BR)" {
    set kbd="keytable=br"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Russian" {
    set kbd="keytable=ru"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Serbian" {
    set kbd="keytable=sr"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Slovak" {
    set kbd="keytable=sk"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Slovene" {
    set kbd="keytable=si"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Spanish" {
    set kbd="keytable=es"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Spanish (Latin America)" {
    set kbd="keytable=latem"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Swedish" {
    set kbd="keytable=se"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Tajik" {
    set kbd="keytable=tj"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Turkish" {
    set kbd="keytable=tr"
    export kbd
    configfile $prefix/grub.locale.cfg
}

menuentry "Ukranian" {
    set kbd="keytable=ua"
    export kbd
    configfile $prefix/grub.locale.cfg
}
